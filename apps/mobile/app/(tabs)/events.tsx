import { SafeAreaView, Text } from 'react-native'
import { centerGrow } from '@/style'

export default function Index() {
  return (
    <SafeAreaView style={[centerGrow]}>
      <Text>EVENTS PAGE</Text>
    </SafeAreaView>
  )
}
